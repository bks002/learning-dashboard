# API base URL used by the frontend fetch client.
# Dev default is /api (proxied to http://localhost:5004 by Vite — see vite.config.ts).
# Override for production builds or when the API runs on a different host.
VITE_API_BASE_URL=/api
